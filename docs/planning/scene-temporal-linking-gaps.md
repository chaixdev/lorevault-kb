# Scene temporal linking loses LLM signal across persistence and chapter boundaries

**Status:** PARKED

## Summary

Scene temporal linking currently fails to preserve or use important LLM-derived chronology information.

The strongest current finding is a pipeline-shape mismatch.

The expected mental model is roughly:

1. chapter ingestion starts
2. scenes are segmented for the chapter
3. scenes are persisted as durable graph nodes
4. triads are built from those persisted scenes
5. later temporal-linking work uses those persisted scene identities

The current implementation does not behave cleanly like that.

Instead, important parts of triad analysis and triad-edge application still depend on in-memory or chapter-local assumptions, while cross-chapter references require persisted scene identities at a broader scope.

That mismatch, combined with a separate read-time chapter-scoping limitation, makes cross-chapter temporal links either fail to materialize or remain ineffective even if they do exist.

## Problem

The ingestion pipeline is intended to use scene triad analysis to infer temporal relationships between scenes, including chapter-boundary cases such as the last scene of one chapter and the first scene of the next.

Today, the graph tends to end up with structural adjacency or heuristic `MEETS`-like results as the durable visible outcome, while stronger LLM-derived temporal hints are either dropped before durable persistence or ignored by downstream consumers.

This creates two related gaps:

- expected cross-chapter temporal links do not reliably appear as durable graph relationships
- temporal hints generated by LLM-assisted detection do not meaningfully influence downstream ordering behavior

## Product Context

- Temporal sequencing is part of the value of scene-as-event modeling.
- If cross-chapter chronology is not preserved, timeline queries and higher-level reasoning become less trustworthy.
- If heuristic defaults dominate over LLM-derived signals, the system appears to have richer chronology analysis than it actually uses.
- This weakens operator confidence when inspecting Neo4j or trying to reason about story flow across chapter boundaries.

## Technical Context

The investigation focused on the scene-detection ingestion path and the temporal ordering read path.

Key files examined:

- `lorevault-api/src/main/java/com/lorevault/api/ai/SceneDetectionService.java`
- `lorevault-api/src/main/java/com/lorevault/api/ai/TriadBuilderService.java`
- `lorevault-api/src/main/java/com/lorevault/api/ai/TriadOrchestrationService.java`
- `lorevault-api/src/main/java/com/lorevault/api/ai/SceneProcessingService.java`
- `lorevault-api/src/main/java/com/lorevault/api/ingestion/SceneDetectionHandler.java`
- `lorevault-api/src/main/java/com/lorevault/api/timeline/TriadEdgePersistenceService.java`
- `lorevault-api/src/main/java/com/lorevault/api/timeline/TemporalEdgeWriteRepository.java`
- `lorevault-api/src/main/java/com/lorevault/api/timeline/TemporalReadRepository.java`
- `lorevault-api/src/main/java/com/lorevault/api/timeline/EventOrderingService.java`

Current code-path findings:

1. `SceneDetectionService.performFullSceneDetection(...)` creates a synthetic `Chapter` plus temporary in-memory `Scene` objects for triad analysis.
2. That same flow runs `triadOrchestrationService.analyzeChapterTriadsWithIndividuals(...)` and immediately calls `triadEdgePersistenceService.applyTriadAnalyses(...)`.
3. Only later, `SceneDetectionHandler.detectAndPersistScenes(...)` calls `sceneProcessingService.persistDetectedScenes(...)` to save the real current-chapter scene nodes.
4. `TemporalEdgeWriteRepository.upsertTemporalEdge(...)` uses `MATCH` on both scene endpoints before `MERGE`, so triad edges that reference scenes not yet durable in Neo4j do not materialize.
5. Cross-chapter triad reasoning may therefore exist conceptually or in-memory without having a durable scene-identity path broad enough to write those edges reliably.
6. `TemporalReadRepository.findChapterEventEdges(...)` only returns edges where both endpoints belong to the same chapter.
7. `EventOrderingService.orderBookEventsUpToChapter(...)` concatenates chapter-local orders rather than building a cross-chapter temporal graph.

## Scope

- Preserve the current investigation in resumable form.
- Track the coupled defects affecting scene temporal linking.
- Capture the known interaction between triad analysis, scene persistence, heuristic defaults, and chapter-scoped reads.
- Frame the work so a later brainstorm or implementation pass can separate write-path fixes from read-path fixes.

## Out of Scope

- Choosing the final implementation strategy
- Refactoring the full ingestion pipeline
- Redesigning the entire timeline model
- Canonical documentation of how temporal linking works after a fix lands

## Known Constraints / Prior Findings

### Primary/root defect: triad analysis is treated as a pre-persistence step

- `SceneDetectionService` creates temporary scenes with fresh UUIDs for triad work.
- `TriadEdgePersistenceService` attempts to persist triad relations before those current-chapter scenes are saved as durable graph nodes.
- `TemporalEdgeWriteRepository.upsertTemporalEdge(...)` requires both scene nodes to already exist in Neo4j.
- As a result, LLM-derived triad relations involving current-chapter scenes can fail silently at write time.

In other words, the implementation still behaves as if temporal-link materialization can happen from a chapter-local pre-persistence context, while the expected cross-chapter model really needs persisted scene identities at the right granularity first.

### Cross-chapter triad setup is also incomplete in the ingestion path

- The synthetic `Chapter` created for triad analysis has only its `id` explicitly populated in `SceneDetectionService`.
- `TriadBuilderService.resolveCrossChapterPreviousScene(...)` requires `bookId` and `chapterNumber` to locate the previous chapter.
- In this path, that means cross-chapter previous-scene resolution can fail before durable persistence is even attempted.

### Segmentation-level chronology hints are dropped before they can help triad reasoning

- `SceneProcessingService.extractSceneResults(...)` parses chronology-related fields such as `chronology`, `chronologyCertainty`, and `chronologyMarker`.
- `localizeSceneCoordinates(...)` reduces that data into `SceneWithCoordinates`, which does not carry those temporal fields forward.
- `TriadOrchestrationService.buildUserVars(...)` currently passes empty `*_time_indicators` and `*_break_reason` values.

### Secondary/masking defect: read-time ordering is chapter-scoped

- `TemporalReadRepository.findChapterEventEdges(...)` only returns same-chapter precedence edges.
- `EventOrderingService.orderChapterEvents(...)` builds adjacency only from the chapter’s own scenes.
- `EventOrderingService.orderBookEventsUpToChapter(...)` concatenates chapter-local orderings rather than letting cross-chapter temporal edges influence the graph.

### Heuristic defaults can mask the earlier failure

- After scene persistence, `SceneDetectionHandler` calls `DefaultTemporalEdgeService.createAllDefaults(bookId)`.
- That later path creates heuristic `R:temporal.meets` edges from persisted scenes.
- Those default edges are therefore more likely to appear in Neo4j than triad-derived temporal edges, making the problem look like a modeling or visibility issue when the first failure happens earlier.

### Relationship between the defects

- These are not fully independent bugs.
- The write-time and cross-chapter-generation problems share the same broad design assumption: triad analysis is performed in-memory before the current chapter’s scenes exist as durable graph nodes at the scope needed by later cross-chapter materialization.
- The read-time issue is a separate but aligned design decision: chapter-scoped ordering means cross-chapter temporal semantics remain ineffective even if durable links are later present.

So the overall issue is best understood as a mismatch between:

- the expected pipeline shape (persisted scenes first, then triad-driven temporal materialization over durable identities)
- and the implemented pipeline shape (triad work and triad-edge application still partly depend on pre-persistence and chapter-local assumptions)

## Open Questions

- Should triad-derived temporal edges be treated as part of scene persistence, a follow-up stage after persistence, or a separate reconciliation step?
- Should cross-chapter temporal reads be supported at the chapter level, the book level, or both?
- How should heuristic default edges interact with stronger LLM-derived temporal relations when both exist?
- Which downstream consumers actually need cross-chapter temporal semantics beyond ordering?
- Is the loss of segmentation chronology fields materially harming triad quality, or is the bigger issue simply that triad outputs never become durable edges?

## Success Criteria

- We can reliably preserve intended LLM-derived temporal information long enough for it to become durable graph data.
- Cross-chapter scene relationships can be generated and verified in the ingestion path.
- Downstream temporal ordering can meaningfully use cross-chapter temporal links where appropriate.
- Heuristic defaults no longer obscure whether richer temporal analysis succeeded or failed.
- A later implementation pass can address the write-path and read-path issues separately without rediscovering the same investigation context.

## Links

- Planning README: `README.md`
- Related implementation files:
  - `../../lorevault-api/src/main/java/com/lorevault/api/ai/SceneDetectionService.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/ai/TriadBuilderService.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/ai/TriadOrchestrationService.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/ai/SceneProcessingService.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/ingestion/SceneDetectionHandler.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/timeline/TriadEdgePersistenceService.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/timeline/TemporalEdgeWriteRepository.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/timeline/TemporalReadRepository.java`
  - `../../lorevault-api/src/main/java/com/lorevault/api/timeline/EventOrderingService.java`
